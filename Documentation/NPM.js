/*

ncu //Sprawdź dostępne aktualizacje:
ncu -u //Zaktualizuj package.json do najnowszych wersji:
ncu --dep=dev -u //Aktualizacji zależności deweloperskich:

npm install
npm list -g --depth=0 // Globalne zależnośći --depth=0: Wyświetla tylko główne pakiety, bez ich zależności.

*/
